/// Налоговые константы режима НПД (налог на профессиональный доход).
///
/// Параметры налогового поля, используемые калькулятором налога:
/// лимит годового дохода, ставка налога и фиксированная часть страховых
/// взносов ИП, которая вычитается из начисленного налога.
class TaxConstants {
  /// Лимит годового дохода для сохранения режима НПД (в рублях).
  static const double limit = 2400000;

  /// Ставка налога 6% при расчёте от дохода.
  static const double rate = 0.06;

  /// Фиксированная часть страховых взносов ИП (в рублях), вычитаемая из налога.
  static const double fixedInsurancePremium = 49500;
}
